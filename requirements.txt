# CageMetrics - Behavioral Analysis App
# Dependencies (same as PhysioMetrics plethapp environment)

PyQt6>=6.4.0
matplotlib>=3.7.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
openpyxl>=3.1.0  # For reading Excel files (fallback)
python-calamine>=0.1.0  # Fast Excel reader (recommended)
